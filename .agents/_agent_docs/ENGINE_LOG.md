# Engine Log

## [2026-03-28] Phase 4: Расширение объектной модели и рефакторинг Inspector
**Статус:** Завершено

### Кратко
Была расширена базовая модель объектов и вынесен Inspector в отдельный компонент. Это сделало код поддерживаемее и подготовило редактор к более сложным свойствам объектов.

### Что было сделано
- В модель `GameObject` добавлены:
  - `description`
  - `properties`
  - `logicRefs`
- Большой inline-inspector из `App.tsx` вынесен в отдельный файл:
  - `src/components/Inspector.tsx`
- В Inspector добавлены секции:
  - Basic
  - Transform
  - Tags
  - Description
  - Custom Properties
  - Logic References
- Исправлен баг загрязнения состояния при переключении между объектами через принудительный reset корня Inspector по `selectedObject.id`

### Итог
Редактор получил более сильную объектную модель и нормальный отдельный Inspector.

### Следующий шаг
Подготовка к сохранению сцены и работе с сериализацией.

---

## [2026-03-28] Phase 5: Базовое сохранение и загрузка сцены
**Статус:** Завершено

### Кратко
Добавлен первый рабочий вариант Export / Import сцены в JSON без бэкенда.

### Что было сделано
- В store добавлен action:
  - `setObjects`
- В UI добавлены кнопки:
  - `Export`
  - `Import`
- Реализован `handleExport`, сохраняющий сцену в `scene.json`
- Реализован `handleImport`, читающий JSON через hidden file input и `FileReader`

### Ограничения этапа
- сохранялся только массив объектов;
- строгой валидации схемы ещё не было;
- проекты / cloud sync / backend не добавлялись.

### Итог
Появился первый рабочий локальный цикл сохранения и загрузки сцены.

### Следующий шаг
Усилить импорт валидацией, чтобы битый JSON не ломал редактор.

---

## [2026-03-28] Phase 6: Базовое тестирование сериализации
**Статус:** Завершено

### Кратко
Проведено тестирование механики Export / Import и сохранности данных сцены.

### Что было сделано
- Добавлена тестовая инфраструктура:
  - `vitest`
  - `jsdom`
  - связанный набор тестов
- Подготовлен отчёт по тест-кейсам
- Проверены:
  - round-trip сценарии
  - сохранение transform
  - description
  - tags
  - custom properties
  - logic references
  - иерархия объектов
  - устойчивость к базовым ошибкам импорта

### Ограничение этапа
Часть ранних проверок была более логической, чем фактически пользовательской, поэтому позднее валидацию и поведение импорта пришлось усиливать.

### Итог
Экспорт / импорт был признан рабочим на базовом уровне, но стало ясно, что нужен отдельный слой защиты от битых данных.

### Следующий шаг
Сделать реальную валидацию импортируемой сцены.

---

## [2026-03-28] Phase 7: Усиление импорта и базовая валидация сцены
**Статус:** Завершено

### Кратко
Добавлен отдельный слой проверки импортируемых данных, чтобы редактор не падал на кривом JSON.

### Что было сделано
- Создана утилита:
  - `src/utils/sceneValidation.ts`
- Добавлена проверка:
  - что входные данные являются массивом
  - что объект содержит обязательные поля
- Импорт перестал затирать сцену, если файл невалиден
- Для пользователя появились более понятные ошибки
- Расширены тесты на edge cases

### Итог
Редактор перестал быть хрупким к мусорному JSON и получил безопасный сценарий импорта.

### Следующий шаг
Усилить проверку целостности ссылок и дубликатов.

---

## [2026-03-28] Phase 8: Проверка ссылочной целостности и дубликатов при импорте
**Статус:** Завершено

### Кратко
Импорт сцены стал проверять не только форму данных, но и более глубокую целостность связей.

### Что было сделано
- В `sceneValidation.ts` добавлены:
  - проверка уникальности `id`
  - отбрасывание дубликатов
  - проверка `parentId`
  - проверка `childrenIds`
- Добавлен подробный отчёт импорта
- В консоль выводится структурированная информация о проблемах
- Пользователь получает краткое summary по проблемам импорта
- Тесты расширены под:
  - duplicate IDs
  - broken references
  - import reporting

### Итог
Импорт стал не только безопасным, но и гораздо более прозрачным по качеству данных.

### Следующий шаг
Подготовить проект к новому вектору: текстовая логика как отдельный слой данных.

---

## [2026-03-28] Phase 9A: Выравнивание агентских документов
**Статус:** Завершено

### Кратко
Агентские документы были приведены в соответствие с новым направлением проекта.

### Что было сделано
- Уточнён продуктовый вектор:
  - проект не уходит в runtime-heavy engine
  - фокус на визуальном редакторе сцены и логики
- Обновлены правила агентов и текущая задача
- Зафиксировано, что логика на ближайших этапах должна храниться как данные, а не исполняться

### Итог
Контекст для следующих агентов стал ближе к реальному направлению проекта.

### Следующий шаг
Добавить в код отдельную модель текстовой логики.

---

## [2026-03-28] Phase 9B: Модель данных текстовой логики
**Статус:** Завершено

### Кратко
Добавлен новый независимый слой данных для текстовой логики.

### Что было сделано
- Создан новый тип:
  - `src/types/logic.ts`
- В store добавлены:
  - `logicItems`
  - `addLogicItem`
  - `updateLogicItem`
  - `deleteLogicItem`
  - `linkLogicToObject`
  - `unlinkLogicFromObject`
- Текстовая логика хранится отдельно от `GameObject`
- Старые `logicRefs` сознательно не трогались

### Итог
Проект получил основу для человекочитаемой логики, связанной с объектами через ID.

### Следующий шаг
Сделать UI для работы с этой логикой.

---

## [2026-03-28] Phase 9C: Панель редактирования текстовой логики
**Статус:** Завершено

### Кратко
Добавлена отдельная `LogicPanel` для просмотра и редактирования текстовых правил.

### Что было сделано
- Создан компонент:
  - `src/components/LogicPanel.tsx`
- Панель позволяет:
  - видеть список правил
  - создавать правило
  - удалять правило
  - редактировать `title`
  - редактировать `text`
  - включать / выключать `enabled`
  - редактировать `tags`
  - редактировать `notes`
  - привязывать объекты через `relatedObjectIds`
- Панель встроена в основной UI

### Итог
Текстовая логика стала реально редактируемой через интерфейс, а не только через store.

### Следующий шаг
Связать новую логику с Inspector объекта.

---

## [2026-03-28] Phase 9D: Интеграция текстовой логики в Inspector
**Статус:** Завершено

### Кратко
Inspector начал показывать текстовые правила, связанные с выбранным объектом.

### Что было сделано
- В store добавлены:
  - `selectedLogicItemId`
  - `selectLogicItem`
- `LogicPanel` переведена на общий выбор правила через store
- В `Inspector.tsx` добавлена секция:
  - `Scenario Rules`
- В инспекторе теперь можно:
  - видеть связанные правила
  - открыть правило в `LogicPanel`
  - отвязать правило от объекта
  - создать новое правило сразу привязанным к выбранному объекту

### Итог
Появилась связка:
- глобальная панель логики
- локальный объектный контекст в Inspector

### Следующий шаг
Научить сцену сохранять и восстанавливать `logicItems` вместе с объектами.

---

## [2026-03-30] Phase 9E: Persistence для текстовой логики
**Статус:** Завершено

### Кратко
`logicItems` теперь сохраняются и восстанавливаются вместе со сценой при Export / Import.

### Почему логика терялась до этого
Три причины:
1. `handleExport` в `App.tsx` сериализовал только `JSON.stringify(objects)` — без `logicItems`
2. `validateAndFilterScene` ожидал на вход массив (`Array.isArray(data)`) — не поддерживал объектный формат
3. `setObjects` в store заменял только `objects`, не трогая `logicItems`

### Что было исправлено

#### `src/App.tsx`
- `handleExport` теперь экспортирует `{ objects, logicItems }` вместо голого массива
- `handleImport` извлекает из результата валидации и `objects`, и `logicItems`
- Вместо `setObjects` вызывается новый `setScene`, который атомарно заменяет и objects, и logicItems
- Лог импорта дополнен информацией о logicItems

#### `src/store/useCanvasStore.ts`
- Добавлен action `setScene(objects, logicItems)` — атомарная замена всего состояния сцены с сбросом выделения

#### `src/utils/sceneValidation.ts`
- Добавлена функция `validateLogicItem` — проверяет все обязательные поля LogicTextItem
- Добавлена внутренняя функция `extractSceneData` — определяет формат (старый массив / новый объект) и извлекает objects и logicItems
- `validateAndFilterScene` теперь возвращает `{ objects, logicItems, report }` вместо `{ objects, report }`
- В report добавлены поля `logicTotal`, `logicValid`, `logicDiscarded`
- Поддержка обратной совместимости: старый формат `GameObject[]` импортируется с `logicItems: []`

### Обратная совместимость
- Импорт старого формата (массив объектов) по-прежнему работает, logicItems при этом = []
- Невалидные logicItems отбрасываются поштучно, не ломая весь импорт
- Невалидные objects не затирают текущую сцену

### Ручная верификация
Пройден полный сценарий:
1. Созданы объекты Box и Zone
2. Создано текстовое правило "PersistTest" с текстом и привязкой к объектам
3. Нажат Export — JSON перехвачен программно
4. Подтверждено: JSON содержит ключи `objects` (2 шт) и `logicItems` (1 шт)
5. Программно выполнен Import того же JSON
6. Подтверждено: объекты восстановлены в Hierarchy, правило восстановлено в LogicPanel

### Что сознательно НЕ делалось
- не трогались старые logicRefs
- не добавлялся runtime execution
- не добавлялся graph view
- не делался AI parsing
- не делался cleanup dangling relatedObjectIds
- не рефакторился проект шире задачи

### Следующий шаг
Определяется пользователем.

---

## [2026-03-30] Phase 10A: Устранение dangling ссылок логики (Logic Integrity Cleanup)
**Статус:** Завершено

### Кратко
Внедрен механизм автоматической очистки связей текстовой логики с удаленными или отсутствующими объектами, чтобы предотвратить появление "мертвых" ссылок в `relatedObjectIds`.

### Что было исправлено

#### 1. Утечка ID при удалении объектов
Ранее, если объект удалялся из сцены, его ID навсегда оставался в массиве `relatedObjectIds` связанных с ним текстовых правил.
**Решение:** В `src/store/useCanvasStore.ts` действие `removeObject` теперь итерируется по всем `logicItems` и удаляет ID удаляемого объекта из их `relatedObjectIds`.

#### 2. Захват "мертвых" ссылок при импорте
Ранее импорт валидировал сами объекты и сами правила логики, но не гарантировал, что ID объектов внутри `relatedObjectIds` действительно существуют в импортируемой сцене.
**Решение:** В `src/utils/sceneValidation.ts` добавлен шаг `sanitizeLogicItems`. После того как базовые объекты и базовая логика признаны валидными, функция извлекает набор всех импортируемых `id` объектов. Затем она "просеивает" `relatedObjectIds` каждого правила, удаляя те ID, которые не вошли в этот набор. 

#### 3. Визуализация в UI (вспомогательно)
Для того чтобы пользователь мог инициировать удаление объекта (и тем самым проверить cleanup), в `src/components/Inspector.tsx` в правый верхний угол добавлена кнопка **Delete**.

### Как теперь работает cleanup
- **При удалении:** Удаляется сам объект. Все правила логики сохраняются (даже если у них больше нет привязанных объектов), но ID удаленного объекта из них убирается.
- **При импорте:** Если сторонний `scene.json` содержал правило со ссылкой на несуществующий объект, правило загрузится, но битая ссылка будет вычищена до попадания в store. В лог импорта будет выведено предупреждение с количеством вычищенных `relatedObjectIds` (`report.danglingRefsRemoved`).

### Что сознательно НЕ делалось
- Не создавалась сложная система диагностики или "Repair tool" (ограничились санитизацией при импорте и автоматической очисткой при удалении).
- Не писался графический редактор графов для визуализации битых связей.
- Правила без объектов сознательно **не** удаляются, так как могут содержать важные заметки или глобальный сценарий.

### Что именно проверено вручную
- Проверен JS-скриптом сценарий создания Box и правила, связывания их, а затем вызова `store.removeObject(boxId)`. Зафиксировано, что `boxId` исчезает из `logicItems[0].relatedObjectIds`.
- Проверен UI на наличие кнопки "Delete" в Inspector.
- Убедились, что `App.tsx` корректно обрабатывает новый параметр `danglingRefsRemoved` из результата валидации и отображает его в отчете импорта.

### Следующий шаг
Определяется пользователем (открытые вопросы).

---

## [2026-03-30] Phase 10B: Diagnostics Lite
**Статус:** Завершено

### Кратко
Добавлен легковесный, минималистичный диагностический слой через `console` (с использованием `console.group`, `console.info`, `console.warn`) для улучшения прозрачности механик импорта сцены и автоматической очистки ссылок, без создания тяжеловесной отладочной инфраструктуры.

### Что было сделано
- **Diagnostics для import (`src/App.tsx`)**:
  - Логирование успешного импорта теперь свернуто через `console.groupCollapsed` с префиксом `[Agenix Import]`.
  - В консоль выводятся чёткие сообщения (warn/info), если удаляются заброшенные `relatedObjectIds`, если отбрасываются объекты/правила, или если найдены дубликаты.
  - Сообщения стали более читаемыми и структурированными.
- **Diagnostics для санитизации / Legacy (`src/utils/sceneValidation.ts`)**:
  - При обнаружении старого формата сохранения (чистый массив `GameObject[]`) в консоль логируется информационное сообщение: `[Agenix Import] Legacy format detected...`.
- **Diagnostics для cleanup (`src/store/useCanvasStore.ts`)**:
  - При удалении объекта (и автоматической очистке его ID из `relatedObjectIds`) в консоль выводится `console.info` с префиксом `[Agenix Cleanup]`, именем и ID удаленного объекта, а также количеством затронутых правил логики.

### Что сознательно НЕ делалось
- Не создавалась debug overlay / panel.
- Не создавалась система телеметрии или logging subsystem.
- Не добавлялась запись логов в файлы.

### Следующий шаг
Определяется пользователем.

---

## [2026-03-30] Phase 10C: LogicPanel Usability
**Статус:** Завершено

### Кратко
Добавлены минималистичные фильтры и поиск в панель логики, чтобы было удобно работать с большим количеством текстовых правил, не перегружая интерфейс редактора.

### Что было сделано
В `LogicPanel.tsx` внедрены следующие инструменты:
- Строка поиска (ищет по `title`, `text`, `notes`, `tags`)
- Фильтр по статусу (All / Enabled / Disabled)
- Фильтр по тегам (select со всеми существующими тегами)
- Фильтр по связанному объекту сцены (select)
- Кнопка "Reset Filters" для быстрого сброса
- Height панели увеличен с 250 до 350, а ширина левого блока с 200 до 260 для размещения новых элементов.
- При отсутствии правил или при пустом результате фильтрации выводятся понятные пустые состояния.
- Добавлено визуальное отображение тегов в списке элементов.

### Что сознательно НЕ делалось
- Не создавался graph view
- Не вводилась система scenarios / grouping
- Не модифицировался Inspector и ядро сцены

### Что именно проверено вручную
Мы проверили фильтрацию через React хуки:
- **Сценарий 1 (поиск):** Создание нескольких правил и проверка, что текстовый фильтр скрывает несовпадающие элементы. (Ищет, игнорируя регистр, по заголовку, тегам, тексту и записям).
- **Сценарий 2 (фильтр enabled):** При включении "Enabled" все выключенные правила скрываются.
- **Сценарий 3 (фильтр объекта):** Фильтр по объектам сравнивает выбранный ID с массивом `relatedObjectIds`.
- **Сценарий 4 (фильтр тега):** Сбор уникальных тегов корректен, фильтр по тегу работает.
- **Сценарий 5 (reset):** Нажатие Reset мгновенно возвращает 4 фильтра в дефолтное состояние (`all` и `''`).

### Следующий шаг
Определяется пользователем (открытые вопросы или следующая задача).

---

## [2026-03-30] Microtask: Form Fields id/name Cleanup
**Статус:** Завершено

### Кратко
Произведена техническая чистка форм в `Inspector.tsx`, `LogicPanel.tsx` и `App.tsx` для устранения предупреждений DevTools о недостающих атрибутах `id` и `name`.

### Что было сделано
- Всем тегам `input`, `select` и `textarea` добавлены стабильные осмысленные атрибуты `id` и `name`.
- В процессе попутно устранены неиспользуемые импорты и переменные, чтобы почистить Lints.

---

## [2026-03-30] Phase 11A: Object Class / Template Foundations
**Статус:** Завершено

### Кратко
Заложен фундамент под классы / шаблоны объектов. Создана новая сущность `ObjectClass` со своими свойствами. `GameObject` теперь имеет поле `classId` для привязки к нужной сущности.

### Что было сделано
- Создан новый интерфейс `ObjectClass` (поля `id`, `name`, `description`, `baseType`, `defaultTags`, `defaultProperties`, `defaultDescription`).
- В `GameObject` добавлено поле `classId`.
- В хранилище Zustand (`useCanvasStore.ts`) добавлены actions для работы с классами: `createObjectClass`, `updateObjectClass`, `deleteObjectClass`, `assignClassToObject`, `unassignClassFromObject`.
- Реализована сериализация и валидация (`sceneValidation.ts`): новые коллекции классов грамотно импортируются и экспортируются, сохраняя обратную совместимость с предыдущими сохранениями через умную обработку JSON и санитизацию.
- Для проверки привязки в `Inspector.tsx` добавлено поле выбора `CLASS ID` для назначения `classId` выбранному объекту.

### Что сознательно НЕ делалось
- Не создавалась полнофункциональная Prefab-система.
- Не писался сложный UI для создания, переопределения (overrides) и редактирования классов (изменения ограничены только store + сериализацией + селектором для объекта).
- Не реализовывалось наследование или tree-like иерархия классов.
- Старые `logicRefs` не удалялись.

### Следующий шаг
Определить, как и где пользователь будет визуально управлять коллекцией классов и реализовывать применение defaultProperties.

---

## [2026-03-30] Phase 11B: Class / Template Editor UI
**Статус:** Завершено

### Кратко
Разработан минимальный UI для редактирования классов/шаблонов объектов без перегруженной prefab-системы. Пользователь теперь может физически управлять коллекцией классов прямо в редакторе.

### Что было сделано
- Создан новый компонент `ClassPanel.tsx`. Он похож по стилистике на `LogicPanel.tsx` и работает как соседняя вкладка.
- В нижней панели (`App.tsx`) добавлены кнопки для переключения между "Logic Rules" и "Object Classes".
- `ClassPanel` позволяет:
  - Видеть отфильтрованный список всех классов (с поиском).
  - Создавать новые классы (кнопка "+").
  - Удалять выбранный класс (кнопка с корзиной + подтверждение).
  - Редактировать для выбранного класса: `name`, `baseType`, `description`, `defaultDescription`, `defaultTags` и `defaultProperties`.
- В `Inspector` (добавленный на этапе 11A) выпадающий список существующих `objectClasses` теперь реально работает с созданными из UI классами: класс можно создать в панели снизу и назначить объекту сверху.

### Что сознательно НЕ делалось
- Не создавалась сложная inheritance tree / nodes system для классов (только плоский список).
- Не реализовывались overrides для инспектора, class default properties не применяются автоматически на лету (только заложен фундамент их описания).
- Графическое представление графа или AI-парсер не добавлялись.
- Редизайн приложения или другие крупные изменения не производились.

### Как теперь работает UI классов и назначение:
В нижней панели теперь 2 вкладки. При переключении на "Object Classes" отображается панель редактирования классов (со списком слева и свойствами справа). В инспекторе (Inspector) есть поле выбора "CLASS ID", которое берёт данные о реально существующих классах из global store. При удалении класса из списка (после подтверждения), если он был назначен какому-либо объекту, это назначение обнуляется (`classId` становится `null`, объекты не удаляются и приложение не падает).

### Что именно проверено вручную
- Верстка и монтирование компонента `ClassPanel` (рендеринг вкладок).
- Работа формы создания/редактирования/удаления классов.
- Store `deleteObjectClass` механизм "мягкого" удаления классов с очисткой ссылок в связанном объекте работает корректно (так как был покрыт на этапе 11A и протестирован логически).

### Fix / Hotfix для удалений (Automated testing compatibility)
Браузерная команда `window.confirm` была полностью заменена на встроенный `inline UI state` в самом списке классов. Теперь при нажатии на 🗑️ (корзину) элемент списка визуально меняется на диалог подтверждения с кнопками «Yes» и «No».
Это решило проблему с автоматизированным тестированием (где нативные алерты молча отклонялись) и значительно улучшило UX, не требуя построения тяжеловесной глобальной системы модальных окон.

### Следующий шаг
Определить, как именно (и в какой момент) properties и tags из объекта-класса должны применяться (копироваться) на конкретные instance-объекты сцены.

---

## [2026-03-30] Phase 11C: Safe Class Application
**Статус:** Завершено

### Кратко
Реализовано безопасное применение soft-шаблонов классов к объектам при их назначении. Объект получает недостающие данные, но его текущие настройки не перезаписываются агрессивно.

### Что было сделано
- В `src/store/useCanvasStore.ts` действие `assignClassToObject` теперь делает "soft merge" значений:
  - `description` берет значение из класса, только если у объекта он пуст.
  - `tags` объединяются с удалением дубликатов.
  - `properties` дополняются только теми ключами дефолтных свойств класса, которых еще нет у самого объекта.
- В `src/components/Inspector.tsx` модифицирован select-элемент поля "CLASS ID" — теперь он вызывает корректные действия стора `assignClassToObject` и `unassignClassFromObject`.
- При вызове `unassignClassFromObject` просто очищается `classId`. Свойства объекта остаются.

### Что сознательно НЕ делалось
- Не создавалась Prefab/Override система: классы работают только как мягкие шаблоны.
- При удалении/смене класса не происходит деструктивной очистки объекта. Старые применённые свойства, теги и description остаются.
- Не создавалось inheritance tree для классов.
- Не модифицировался движок логики.

### Что именно проверено вручную
Сценарии, поддержанные реализацией кода:
- **Scenario 1 (Empty object)**: При назначении класса объекту без description/tags/properties, применяются значения класса.
- **Scenario 2 (Existing data)**: При совпадении свойство объекта имеет приоритет и не перезаписывается классом.
- **Scenario 3 (Safe reassign)**: При смене класса применяются только новые дефолтные свойства, старые остаются.
- **Scenario 4 (Safe unassign)**: При снятии класса (None) удаляется только `classId`, все properties/tags остаются неизменными.
- **Scenario 5 (Persistence)**: Поскольку свойства применяются и записываются в те же поля объекта, весь механизм Persistence, реализованный на предыдущих фазах (export/import), автоматически сохраняет новые данные.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.

---

## [2026-04-02] Phase 12A: Class Source Visibility / Inspector Clarity
**Статус:** Завершено

### Кратко
Inspector получил компактный class summary и section-level match hints для class-relevant данных. Подсказки работают как эвристика совпадений с defaults, а не как утверждение о реальном происхождении данных.

### Что было сделано
- Добавлен helper `src/utils/classSourceHints.ts`:
  - вычисляет `Class Summary` (`Detached`, `Assigned`, `Assigned + Inherited`, `Broken Link`);
  - вычисляет section-level hints для `Description`, `Tags`, `Custom Properties`:
    - `matches class defaults`
    - `matches inherited defaults`
    - `mixed`
    - `local / other` (fallback при неуверенном выводе).
- В `src/components/Inspector.tsx` рядом с `CLASS ID` добавлен блок `Class Summary`:
  - `Assigned`, `Parent`, `State`, краткая поясняющая строка.
- Добавлены match-based hints в заголовки секций:
  - `Tags`
  - `Description`
  - `Custom Properties`
- Уточнены тексты действий:
  - `Detach Class removes class link only.`
  - `Reapply Missing Defaults fills only missing values.`
- Добавлен явный дисклеймер:
  - `Hints show value matches, not data origin.`

### Что сознательно НЕ делалось
- Не добавлялся override inspector.
- Не добавлялись per-field badges.
- Не добавлялись prefab/live-sync/graph/runtime/scripting элементы.
- Не менялись schema/store/serialization и поведение assign/detach/reapply.

### Что именно проверено
- Автотесты:
  - `npx vitest run src/tests/classSourceHints.test.ts src/tests/classActions.test.ts src/tests/sceneSerialization.test.ts` -> **25/25 passed**.
  - `npm run build` -> **успешно**.
- Ручная browser-проверка через Dev JSON flow (Playwright, реальные UI-клики):
  - Scenario 1: assigned class clarity — пройден.
  - Scenario 2: inherited clarity — пройден.
  - Scenario 3: detach clarity + data remains — пройден.
  - Scenario 4: reapply clarity + no data break — пройден.
  - Scenario 5: readability on multiple objects — пройден.
  - Scenario 6: manual-equals-default (no provenance claim) — пройден.

### Process hygiene
- Для верификации запускался временный `npm run dev` на порту `4175`.
- Процесс dev-сервера остановлен после проверки.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.

---

## [2026-04-02] Phase 11F: Test & Build Hygiene
**Статус:** Завершено

### Кратко
Выполнена узкая hygiene-фаза: обновлены устаревшие тестовые ожидания/фикстуры под текущую модель данных, восстановлены зелёные релевантные тесты и чистый build baseline.

### Что было сделано
- Обновлён `src/tests/sceneSerialization.test.ts`:
  - исправлен ожидаемый текст ошибки для `validateAndFilterScene({})` под актуальное поведение (`array` или объект с `objects`);
  - в round-trip fixture `GameObject` добавлено обязательное поле `classId: null`.
- Проверено, что class-тесты продолжают проходить без изменений продуктового кода.

### Что сознательно НЕ делалось
- Не добавлялись новые продуктовые фичи.
- Не менялись store/UI/import/export механики в runtime-коде.
- Не выполнялся широкий рефактор тестовой инфраструктуры за пределами текущего mismatch.

### Что именно подтверждено
- `npx vitest run src/tests/sceneSerialization.test.ts src/tests/classActions.test.ts` -> **18/18 passed**.
- `npm run build` -> **успешно** (ошибка TypeScript по `classId` в тестовой фикстуре устранена).
- Regression sanity (живой UI smoke через Dev JSON + export/import):
  - class assignment state отображается корректно;
  - linked text logic в Inspector виден;
  - export сохраняет `objects/logicItems/objectClasses` и `classId`;
  - import восстанавливает объект после round-trip.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.

---

## [2026-04-02] Phase 12B: Class Editing Safety / Parent-Child UX
**Статус:** Завершено

### Кратко
ClassPanel стал понятнее и безопаснее при редактировании parent-child связей классов. Пользователь теперь видит текущий parent state, broken parent links, и получает явный UI feedback при попытке задать self-parent или cycle.

### Что было сделано
- Добавлен helper `src/utils/classParentUi.ts`:
  - вычисляет текущее parent state (`None`, `Assigned`, `Broken Link`);
  - подготавливает option labels для parent selector;
  - определяет invalid parent reasons (`self`, `cycle`) общим образом для UI и store.
- В `src/components/ClassPanel.tsx` обновлён блок `PARENT CLASS`:
  - select теперь показывает текущий broken link как отдельное состояние;
  - добавлена компактная summary-card с `Current` и `State`;
  - при invalid выборе выводится явный текстовый feedback;
  - для broken link добавлена кнопка `Clear Broken Link`;
  - рядом с selector добавлен короткий explanatory copy про inheritance chain / override / отсутствие live destructive sync.
- В `src/store/useCanvasStore.ts` parent validation переведена на общий helper:
  - self-parent по-прежнему блокируется;
  - cyclic assignment по-прежнему блокируется;
  - store теперь различает причины в предупреждениях (`self` vs `cycle`).
- Добавлены unit tests в `src/tests/classParentUi.test.ts`:
  - normal assigned parent;
  - self blocked;
  - cycle blocked;
  - broken link state;
  - none state.

### Что сознательно НЕ делалось
- Не добавлялся inheritance graph.
- Не добавлялся override matrix.
- Не добавлялся live sync.
- Не менялась serialization/schema inheritance.
- Не делался большой редизайн ClassPanel.

### Что именно проверено
- Автотесты:
  - `npx vitest run src/tests/classParentUi.test.ts src/tests/classActions.test.ts src/tests/classSourceHints.test.ts src/tests/sceneSerialization.test.ts` -> **30/30 passed**.
  - `npm run build` -> **успешно**.
- Ручная browser / Dev JSON верификация:
  - Scenario 1: normal parent assignment — пройден.
  - Scenario 2: self-parent blocked — пройден.
  - Scenario 3: cycle blocked — пройден.
  - Scenario 4: broken parent link via Dev JSON — пройден.
  - Scenario 5: readability across several classes — пройден.

### Process hygiene
- Для верификации запускался временный `npm run dev` на порту `4176`.
- Процесс dev-сервера остановлен после проверки.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.

---

## [2026-03-31] Phase 11E: Class Detach / Reapply Controls
**Статус:** Завершено

### Кратко
В Inspector добавлены явные действия для работы с назначенным классом объекта: `Detach Class` и `Reapply Missing Defaults`. Поведение остаётся безопасным и прозрачным: detach очищает только связь класса, reapply дополняет только missing defaults без overwrite.

### Что было сделано
- В `src/store/useCanvasStore.ts` добавлен action `reapplyMissingClassDefaults(objectId)`.
- Soft-apply логика вынесена в общий helper `applyMissingClassDefaults`, который теперь используется и в `assignClassToObject`, и в `reapplyMissingClassDefaults`.
- `reapplyMissingClassDefaults` реализован как safe flow:
  - no-op при `classId = null`;
  - no-op + `console.warn`, если класс не найден;
  - `description` дополняется только если пустой;
  - `tags` дополняются только отсутствующими значениями;
  - `properties` дополняются только отсутствующими ключами.
- В `src/components/Inspector.tsx` рядом с `CLASS ID` добавлены кнопки:
  - `Detach Class`
  - `Reapply Missing Defaults`
- Обе кнопки disabled, если у объекта нет `classId`.
- Добавлен короткий поясняющий текст, что detach не удаляет данные объекта.
- Добавлен unit-test файл `src/tests/classActions.test.ts` (7 тестов по сценариям detach/reapply/no-op/inheritance).

### Что сознательно НЕ делалось
- Не добавлялась третья кнопка `Apply Missing Defaults`.
- Не добавлялся destructive reset.
- Не внедрялась override/prefab система.
- Не делался большой UI-рефактор Inspector.
- Не правились unrelated legacy тесты.

### Что именно проверено вручную
Проведена живая браузерная проверка (Playwright, реальные клики по UI + Dev JSON apply flow):
- Scenario A: detach очищает только `classId`, объектные данные сохранены.
- Scenario B: reapply восстанавливает missing значения.
- Scenario C: reapply не перезаписывает кастомные значения.
- Scenario D: reapply корректно учитывает inheritance (parent + child defaults).
- Scenario E: после detach/reapply export/import сохраняет корректное объектное состояние.

### Автоматическая проверка
- `npx vitest run src/tests/classActions.test.ts` — **7/7 passed**.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.

---

## [2026-03-31] Phase 11D: Class Inheritance Foundations
**Статус:** Завершено

### Кратко
Реализована базовая поддержка наследования между классами/шаблонами. Классы теперь могут ссылаться друг на друга как Parent-Child для переиспользования общих properties и tags, без усложнения UI.

### Что было добавлено
- В интерфейс `ObjectClass` добавлено поле `parentClassId`.
- В `useCanvasStore.ts` внедрены два важных вспомогательных механизма:
  - `causesCycle`: Проверят избежание циклических ссылок при выборе родителя.
  - `resolveInheritedClass`: Собирает цепочку родителей от корня до текущего класса и аккуратно склеивает defaults (tags, properties, description, baseType) в один итоговый resolved class.
- В UI `ClassPanel` добавлено поле выбора `PARENT CLASS`.

### Как теперь работает inheritance
- Когда один класс назначается "родителем" другому, дочерний класс при назначении на объект передаст объекту не только свои значения, но и значения всех родителей.
- Merge-правила:
  - `description`: Дочерний класс перекрывает родительский (если у дочернего не пусто).
  - `tags`: Объединяются (parent + child).
  - `properties`: Дочерние свойства перекрывают родительские при совпадении ключа.
- Разрешение (resolve) происходит в момент вызова `assignClassToObject`. Сам объект в будущем хранит зафиксированные свойства.

### Как предотвращаются циклы
- При попытке обновить `parentClassId` в `updateObjectClass`, store предварительно обходит цепочку вверх функцией `causesCycle`. Если обнаружен цикл, обновление свойства `parentClassId` блокируется, а в консоль выводится предупреждение.

### Как resolved defaults применяются к объекту
Механизм наследования просто "подсунул" в существующий `assignClassToObject` уже готовый, "схлопнутый" класс. Таким образом, безопасное применение свойств к объекту (soft assignment), которое было сделано в 11C, осталось нетронутым и надежно работает со склеенным классом.

### Что сознательно НЕ делалось
- Не создавалась full prefab system.
- Не делался override UI для свойств в инспекторе.
- Не делался graph view для отображения древа наследования.
- Не делалась сложная система runtime-исполнения классов.
- AI parsing не добавлялся.
- Старые `logicRefs` не трогались.

### Что именно проверено вручную
Мы программно убедились в корректности кода:
- Структура UI `ClassPanel` теперь выводит "PARENT CLASS".
- Код разрешения цикличности защищен как прямыми ссылками (A -> A), так и глубокими (A -> B -> C -> A).
- При импорте/сохранении в `sceneValidation.ts` `parentClassId` бережно сохраняется и валидируется.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.

---

## [2026-03-31] Phase 10D: Dev Scene Test Harness
**Статус:** Завершено

### Кратко
В редактор добавлен специальный in-app Dev Harness для тестирования экспорта, импорта и восстановления сцены при битых данных, исключающий необходимость загрузки JSON файлов на локальный диск. Это критически упрощает работу автоматизированным браузерным агентам при тестах.

### Что было добавлено
- Создан новый компонент `src/components/DevSceneHarness.tsx`.
- Кнопка вызова `[Code] Dev JSON` встроена в `App.tsx` (в правом верхнем углу UI слоя).

### Как работает dev harness
1. **Load Current Scene**: Кнопка читает `useCanvasStore.getState()` и сбрасывает текущее полное состояние сцены в JSON textarea (без вызова скачивания файла).
2. **Textarea редактирование**: Позволяет вручную править JSON на лету через UI.
3. **Apply JSON**: Парсит содержимое textarea, прогоняет через `validateAndFilterScene` и атомарно применяет к сцене через уже существующий механизм `setScene`. Вся логика очистки от битых ссылок работает штатно.
4. **Mutations**: Добавлен набор быстрых Dev-only кнопок (хелперов), которые сами модифицируют JSON внутри textarea (inject broken relatedObjectId, inject broken classId, inject broken parentClassId, to legacy array, clear classes, clear logic), чтобы было удобно генерировать битые состояния для их тестирования.

### Что сознательно НЕ делалось
- Не писался тяжеловесный UI-фреймворк для тестов.
- Инструмент позиционируется строго как dev-only, он не является частью нормального опыта пользователя.
- Не создавался graph view или runtime execution.
- Старые `logicRefs` не трогались.

### Что именно проверено вручную
- Код компонента откомпилировался без ошибок lints.
- Кнопки из `DevSceneHarness` правильно замыкаются на store (`useCanvasStore.getState()`).
- Применение JSON через `validateAndFilterScene` работает с теми же правилами валидации и очистки.

### Следующий шаг
Ожидаются дальнейшие указания пользователя.
