---
description: Current Task
---

Phase 9: Agent Docs Alignment

Цель:
Выровнять агентские документы проекта под новый вектор развития (текстовая логика как данные).

Текущий подэтап:
- Обновить `agent-rules.md`, `agenix-rules.md`, `CURRENT_TASK.md` и `ENGINE_LOG.md`.
- Убрать противоречия и старые данные (Windows-пути, неактуальные ссылки).
- Установить фокус на текстовой логике как на слое данных (без runtime и AI).
- Код приложения в `src/` в этом подэтапе не меняется.

Что пользователь должен получить в конце этапа:
- Агентские документы больше не противоречат друг другу.
- Следующий агент сразу понимает текущее направление: текстовая логика как данные без runtime/ECS.
- `CURRENT_TASK.md` ограничивает только текущий подэтап (Agent Docs Alignment).